version: "3"

services:

  certbot:
    container_name: certbot
    image: certbot/certbot
    command: renew
#    command: certonly --webroot --webroot-path=/var/www/certbot --email lizzy@eclipsegeomatics.com --agree-tos --no-eff-email -d data.skeenasalmon.info -d  www.data.skeenasalmon.info -v
    volumes:
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/logs:/var/log/letsencrypt
      - ./certbot/data:/var/www/certbot